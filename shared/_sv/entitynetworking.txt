--@author ember
--@server

local PLAYERS_ALL = {}

local queue = {}
local function queueHook()

end

function entitynetworking.send(players,key,ents)
    if ents.entIndex then ents = {ents} end

    net.start("kl_en")
    for i = 1,#ents do
        net.writeUInt(ents[i]:entIndex())
    end
    net.send()
end

function entitynetworking.broadcast(key,ents)
    entitynetworking.send(PLAYERS_ALL,key,e)
end

hook.add("EntityRemoved","kl:en_er",function(e)

end)

hook.add("ClientInitialized","kl:en_ci",function(p)

end)

hook.add("PlayerDisconnect","kl:en_pl",function(p)

